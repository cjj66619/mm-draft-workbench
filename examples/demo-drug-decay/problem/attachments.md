# 附件清单

| 文件 | 内容 | 对应子问题 | 存放位置 | 来源 |
| --- | --- | --- | --- | --- |
| `pk_raw.csv` | 3 名受试者 × 12 个采样时刻的血药浓度：`subject`、`time_h`、`conc_mg_L`，36 行，含 1 个缺失、1 个异常高值、1 个负值 | 问题一（拟合）、问题三（清洗前后对比） | `data/raw/pk_raw.csv`（只读） | `code/_make_synthetic_raw.py` 用固定种子 20260906 合成 |
| `pk_raw_truth.json` | 合成数据的“真值”：群体参数、各受试者个体参数、噪声设定、三处人为缺陷的位置 | 仅用于演示项目的自检对照，**不得**在建模中使用 | `data/raw/pk_raw_truth.json` | 同上 |

说明：真实竞赛项目中 `data/raw/` 放官方附件原件，且不会有 `*_truth.json`。
